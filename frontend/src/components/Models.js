import React from 'react';
import { Typography } from '@material-ui/core';

const Models = () => {
  return (
    <div>
      <Typography variant="h4">ML Models</Typography>
      <Typography variant="body1">This is the ML Models page.</Typography>
    </div>
  );
};

export default Models;